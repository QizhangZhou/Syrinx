#include "map_container.h"
using namespace std;

void mapContainerTest()
{
    // multimap<int, int> mapV;
    // mapV.insert(make_pair(10, 10));
    // mapV.insert(make_pair(10, 12));
    // mapV.insert(make_pair(10, 14));
    // // cout << mapV.at(10) << endl;

    // auto findValue = mapV.find(10);
    // auto findCount = mapV.count(10);
    // while(findCount--) {
    //     cout << "findValue : " << findValue->second << endl;
    //     findValue++;
    // }
}